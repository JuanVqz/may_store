<%# locals: (product:) %>
<% if product.available? %>
  <%= render "components/badge", variant: :default do %><%= t("admin.available") %><% end %>
<% else %>
  <%= render "components/badge", variant: :secondary do %><%= t("admin.unavailable") %><% end %>
<% end %>
