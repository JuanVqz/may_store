<% content_for :title, t("app_name") %>

<div class="flex items-center justify-center h-full">
  <div class="text-center">
    <h2 class="text-2xl font-bold"><%= Current.store.name %></h2>
    <p class="text-muted-foreground mt-2"><%= t("login.subtitle") %></p>
  </div>
</div>
